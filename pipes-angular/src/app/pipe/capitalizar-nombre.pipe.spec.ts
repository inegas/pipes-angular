import { CapitalizarNombrePipe } from './capitalizar-nombre.pipe';

describe('CapitalizarNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizarNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
